/** ########### PMD CONFIGURATION  **/

pmd {
    toolVersion = "6.25.0"
    ruleSets = ["${rootProject.projectDir}/config/pmd/pmd-ruleset.xml"]
}	


/** ########### CHECKSTYLE CONFIGURATION  **/

checkstyle {
    toolVersion '7.8.1'
    configFile file("config/checkstyle/checkstyle.xml")
}
checkstyleMain {
    source ='src/main/java'
}
checkstyleTest {
    source ='src/test/java'
}


/** ########### SPOTBUGS CONFIGURATION  **/

spotbugs {
    excludeFilter = new File("config/spotbugs/spotbugs-exclude.xml")
}

spotbugsMain {
    reports {
        html {
            enabled = true
            destination = file("$buildDir/reports/spotbugs/main/spotbugs.html")
            stylesheet = 'fancy-hist.xsl'
        }
    }
}

/** ########### JACOCO CONFIGURATION  **/

ext['basePackage'] = 'src/main/java/co/com/rodriguez/natalia'

test {
    jacoco {
        enabled = true
    }
}

jacocoTestReport {
    reports {
        xml.enabled false
        csv.enabled false
    }
        afterEvaluate {
        classDirectories.from = files(classDirectories.files.collect {
            fileTree(
                dir: it, 
                exclude: [ "${basePackage}/crosscutting/messages/**",
        		   "${basePackage}/infrastructure/main/**",
				   "${basePackage}/crosscutting/domain/**",
				   "${basePackage}/crosscutting/persistence/entity/**",
				   "${basePackage}/crosscutting/persistence/repository/**",
				   "${basePackage}/infrastructure/configuration/**",
				   "${basePackage}/modules/configurations/gateway/model/pki/**" ]                 
                )
        })
    }
}



/** ########### SONARQUBE CONFIGURATION  **/

ext['sonarExcludesPackages'] = [
				   "${basePackage}/crosscutting/messages/**",
                   "${basePackage}/crosscutting/domain/**",
				   "${basePackage}/infrastructure/main/**",
				   "${basePackage}/crosscutting/persistence/entity/**",
				   "${basePackage}/crosscutting/persistence/repository/**",
				   "${basePackage}/infrastructure/configuration/**",
				   "${basePackage}/crosscutting/util/**",				   
                   "${basePackage}/modules/configurations/gateway/model/pki/**",
]

sonarqube {
    properties {
        property "sonar.projectName", "report-payment-service"
        property "sonar.projectKey", "report-payment-service"
        property "sonar.language", "java"
		property "sonar.host.url", "http://10.25.10.212:9000/sonar/"		
        property "sonar.login", "51b2daf67855bdc7933e1115f54db9ffbb503372"
        //property "sonar.host.url", "http://localhost:9000/"
		property "sonar.coverage.exclusions", sonarExcludesPackages
		property "sonar.exclusions", sonarExcludesPackages										   
    }
    
}